version: '3'
services:
  web:
    build: .
    environment:
      - SITE=${SITE}
      - VERIFICATION_TOKEN=${VERIFICATION_TOKEN}
      - DATABASE_URL=${DATABASE_URL}
      - BIP39=${BIP39}
      - SLACK_APP_SECRET=${SLACK_APP_SECRET}
    ports:
     - 8000:8000
